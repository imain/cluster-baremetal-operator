apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: cluster-baremetal-operator
  name: controller-manager-metrics-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    targetPort: https
  selector:
    k8s-app: cluster-baremetal-operator
